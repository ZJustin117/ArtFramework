# Facade Internals Ledger

| ID | Scope | Before | Target after | Evidence before | Evidence after | Tests | Review | Status |
|---|---|---|---|---|---|---|---|---|
| FI-01 | Lifecycle coordination | `ArtFramework` owns tracked handles, aliases, synthetic close, and retirement hook directly | Package-private lifecycle coordinator owns these disposable compatibility mechanics; facade delegates | `ArtFramework.java` tracked-handle and retirement methods | `FrameworkLifecycleCoordinator` owns definitions, definition aliases, handle aliases, collision cleanup, synthetic attach/detach, close, and retirement hook. R2/R3/R4/R5 stale-alias and stale-handle findings are fixed with regression coverage | `ArtFrameworkTest`, `MountHostTest` (unregister/re-register, empty resource cache loss, alias collision and open-handle collision tests), `SyntheticRuntimeTest`; focused gate passed; full 728-test gate passed | R1 findings; R2-01, R3-01, R4-01, R5-01, R5-02 accepted and fixed; R6 final review `ses_fddcda8cbffed5xiFxG5N0M8ck` PASS | complete |
| FI-02 | Schedule compatibility bridge | Facade directly forwards several schedule-owned compatibility operations | Package-private bridge owns forwarding; one `PresentationSchedule` remains the authority | `ArtFramework.java` schedule methods and `PresentationSchedule` | `FrameworkScheduleBridge` delegates advance, publish, synchronous surface/native lifecycle, host presentation, and reset to the single static schedule; negative inventory found one schedule instance and no facade bypass | `PresentationScheduleTest` including facade publish bridge; focused gate and full 728-test gate passed | R1 findings; R6 final review `ses_fddcda8cbffed5xiFxG5N0M8ck` found no actionable schedule finding | complete |
| FI-03 | Test reset coordination | Large reset checklist is embedded in public facade class | Package-private test reset coordinator owns the test-only cleanup checklist | `ArtFramework.resetForTests()` | `FrameworkTestReset` owns the reset checklist; duplicate signal reset calls are present only there; facade only delegates reset and restores native ops backend | `ArtFrameworkTest`, `SignalApiContractTest`; focused gate and full 728-test gate passed | R1 finding FI2-01 fixed; R6 final review `ses_fddcda8cbffed5xiFxG5N0M8ck` found no duplicate reset authority | complete |
| FI-04 | Public compatibility | Facade aliases and consumer-facing signatures must remain unchanged | Same methods delegate to extracted internals without public API changes | `api-stability.md`, consumer fixture | Public methods remain unchanged; extracted coordinators remain package-private; consumer fixture compiled against rebuilt `ArtFramework.jar` | `./scripts/verify-consumer-fixture.sh` passed; focused gate and full 728-test gate passed | R1 scope reviewed; R6 final review `ses_fddcda8cbffed5xiFxG5N0M8ck` found no new consumer-facing API | complete |
