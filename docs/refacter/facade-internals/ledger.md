# Facade Internals Ledger

| ID | Scope | Before | Target after | Evidence before | Evidence after | Tests | Review | Status |
|---|---|---|---|---|---|---|---|---|
| FI-01 | Lifecycle coordination | `ArtFramework` owns tracked handles, aliases, synthetic close, and retirement hook directly | Package-private lifecycle coordinator owns these disposable compatibility mechanics; facade delegates | `ArtFramework.java` tracked-handle and retirement methods | `FrameworkLifecycleCoordinator` owns definitions, handle aliases, synthetic attach/detach, close, and retirement hook; `ArtFramework` retains public delegation only | `ArtFrameworkTest`, `MountHostTest`, `SyntheticRuntimeTest`; full JUnit passed | R1 initial findings reviewed; no lifecycle finding remained in tested scope; continuation result unavailable | complete |
| FI-02 | Schedule compatibility bridge | Facade directly forwards several schedule-owned compatibility operations | Package-private bridge owns forwarding; one `PresentationSchedule` remains the authority | `ArtFramework.java` schedule methods and `PresentationSchedule` | `FrameworkScheduleBridge` delegates advance, publish, synchronous surface/native lifecycle, host presentation, and reset to the single static schedule | `PresentationScheduleTest` including facade publish bridge; `PackWorldTest`; full JUnit passed | R1 initial review requested bridge coverage; coverage added; continuation result unavailable | complete |
| FI-03 | Test reset coordination | Large reset checklist is embedded in public facade class | Package-private test reset coordinator owns the test-only cleanup checklist | `ArtFramework.resetForTests()` | `FrameworkTestReset` owns the reset checklist; duplicate signal reset calls removed from facade; facade only invokes coordinator and restores native ops backend | `ArtFrameworkTest`, `SignalApiContractTest`, full JUnit passed | R1 finding FI2-01 fixed; continuation result unavailable | complete |
| FI-04 | Public compatibility | Facade aliases and consumer-facing signatures must remain unchanged | Same methods delegate to extracted internals without public API changes | `api-stability.md`, consumer fixture | Public methods remain unchanged; consumer fixture compiles against rebuilt `ArtFramework.jar` | `./scripts/verify-consumer-fixture.sh` passed; full JUnit passed | R1 scope reviewed; continuation result unavailable | complete |
