# Native Render Ownership Ledger

| ID | Scope | Before | Target after | Evidence before | Evidence after | Tests | Review | Status |
|---|---|---|---|---|---|---|---|---|
| NRO-01 | Combat hand/card render ownership | `AbstractPlayer.renderHand` can be suppressed and ART hand-builds card pixels via `Sts1HandCardRenderer` / `Sts1VanillaDraw` | Hand/card pixels are drawn by live STS cards through native render functions; ART only syncs pose, projection, input, and overlays | `CombatHandRenderPatches.java`, `Sts1SurfaceRenderer.renderHand`, `Sts1HandCardRenderer.java`, D1 screenshot mismatch | `Sts1HandCardRenderer.java` delegates to `AbstractCard.render`; `Sts1VanillaDraw` hand-built card pixel helpers removed; `Sts1SurfaceRenderer.find` resolves live card by uuid or identity hash; D1 combat verify passed | `Sts1HandCardRendererTest.drawItemBoundsRemainHostNeutral`, `Sts1HandCardRendererTest.returnsZeroWhenNoLiveCardSupplied`, `Sts1HandCardRendererTest.classifiedAsNativeCardAuthoritative`; default JUnit gate 766 tests passed | Pending | complete |
| NRO-02 | Combat controls/energy/intents | End turn, energy, and intent native renders can be suppressed and hand-redrawn by ART | Original native renderers continue; ART overlays are explicit and non-authoritative | Explore audit `ses_fc86b09b2ffeUkSx5KgMQvOd0X` | `CombatControlsRenderPatches.java`, `CombatEnergyRenderPatches.java`, `CombatIntentRenderPatches.java` now observe and `SpireReturn.Continue()`; `SurfaceDrawPlan.java` keeps native pixel authority for these surfaces; `Sts1SurfaceRenderer.renderControls/Energy/Intents` no longer draws replacement pixels; D1 combat verify passed after updating YAML fixture | `ControlsDrawPathTest.neverSuppressesNativeEndTurnEvenWhenFullMountedCombat`, `Sts1RenderPipelineTest.fullMountedControlsDrawWithoutSuppressingNative`, `fullMountedEnergyDrawWithoutSuppressingNative`, `fullMountedIntentsDrawsWithoutSuppressingNative`, `NativeRenderBridgeTest.controlsEnergyIntentsPassThroughInFullModeWithoutSuppressingNative`; JUnit gate passed; D1 combat verify passed | Pending | complete |
| NRO-03 | Map/event/select/room surfaces | Whole native screens/rooms can be suppressed and hand-redrawn by ART | Original native renderers continue; ART observes and overlays only unless no native pixel owner exists | Explore audit `ses_fc86b09b2ffeUkSx5KgMQvOd0X` | `MapRenderPatches`, `EventRenderPatches`, `SelectRenderPatches`, `RoomRenderPatches` now observe and `SpireReturn.Continue()`; `SurfaceDrawPlan` keeps native pixel authority for sts1.map/event/select.grid/select.hand/reward.combat/reward.card/reward.boss_relic/rest/shop/treasure; `Sts1SurfaceRenderer` no longer draws replacement pixels for these surfaces; C2 input items remain synced in prepareXxxVisuals; D1 combat verify passed | `MapDrawPathTest.neverSuppressesNativeMapEvenWhenFullMounted`, `EventDrawPathTest.fullMountedDrawsWithoutSuppressingNative`, `SelectDrawPathTest.fullMountedGridDrawsWithoutSuppressingNative`, `NativeRenderBridgeTest.mapEventSelectRoomSurfacesPassThroughInFullModeWithoutSuppressingNative`, `Sts1RenderPipelineTest.mapEventSelectRoomSurfacesDrawWithoutSuppressingNative`; JUnit gate passed; D1 combat verify passed | Pending | complete |
| NRO-04 | Skeleton/effect ownership | Effects are observe-only, skeleton claims need overlap/recovery proof | Effects remain native queue authority; skeleton delegation is per-instance, tested against double draw | Explore audit `ses_fc86b09b2ffeUkSx5KgMQvOd0X`; Slice 3 follow-up: the old three-arg `AbstractGameEffect.render` Prefix missed container-driven effects (single-arg render is abstract, ~264 classes dispatch at the three `AbstractDungeon.render` call sites) | `SkeletonRenderPatches.java` suppresses `SkeletonMeshRenderer.draw` only for per-instance ART claims and always returns `Return(null)` after a delegated claim; `TransientEffectRenderPatches.java` keeps `CAPTURE_AND_PASS` so the native effect queue continues; `TransientEffectContainerPatches.java` instruments the three `AbstractDungeon#render(SpriteBatch)` call sites with an observe-then-render helper (zero suppression, fail-open ledger note); transient-effect Registry→adapter projection converges into the schedule-owned `TransientEffectProjectionSystem` (`HOST_PRESENTATION`, sync drain keeps same-frame visibility); `SurfaceDrawPlan.java` treats `SKELETON` as native-pixel-authoritative (per-instance claims only); `Sts1SkeletonBridge.java` uses a provider-agnostic `SkeletonNativeSlotRenderer` interface; `Sts1SurfaceRenderer.renderSkeleton` does not hand-draw fallback pixels; D1 combat verify passed | `Sts1SkeletonBridgeTest.unclaimedNativeSkeletonPassesThroughWithNativeContinuation`, `Sts1SkeletonBridgeTest.claimedSkeletonDelegatesWithoutNativeContinuation`, `Sts1SkeletonBridgeTest.hostRecreationRestoresSkeletonClaimWithoutDoubleDraw`, `NativeRenderBridgeTest.transientEffectRenderAlwaysCapturesAndPasses`, `Sts1RenderPipelineTest.fullMountedSkeletonDrawsWithoutSuppressingNative`, `TransientEffectContainerPatchesTest`, `TransientEffectProjectionSystemTest`; JUnit gate passed; D1 combat verify passed | Pending | complete |
| NRO-05 | Manifest/docs/static gate | NRCC docs say no rewrite but still allow broad `DELEGATE_TO_ART`; scanner lacks ownership policy enforcement | Manifest and tests fail on unapproved native suppression or hand-made native pixel replacement; delegated surfaces with minimal/incomplete supply are recorded as `ART_DELEGATED` with exposed pixel-supply gaps, not native-authoritative coverage | `native-render-coverage-sdd.md`, `tools/nrcc` audit | `docs/design/native-render-coverage-sdd.md` native pixel authority rule + `NATIVE_WITH_ART_OVERLAY`; `docs/design/c2-full-present.md` clarified as overlay/input layer; `tools/nrcc/coverage_manifest.py` ownership gate + `NATIVE_WITH_ART_OVERLAY`; `tools/nrcc/manifests/sts1-native-coverage.yaml` regenerated with ownership class + justifications; `RenderPatchOwnershipTest` JUnit regression; `SurfaceDrawPlan.keepsNativePixelAuthority` package-private for test; D1 combat verify passed; first ART coverage slice aligns comments/tests/docs around delegated surfaces with exposed pixel-supply gaps | `RenderPatchOwnershipTest`, `Sts1RenderPipelineTest`, `NativeRenderBridgeTest`; Python `tools/nrcc/tests`; JUnit gate passed; D1 combat verify passed | Pending | complete |
| NRO-06 | End-turn chrome reflection bridge (review finding V-3, gap G1) | Unlisted `Sts1EndTurnChrome` read `AbstractDungeon.overlayMenu.endTurnButton` hb/label via reflection on every render call (`Sts1SurfaceRenderer` controls chrome + C2 item sync) | Reflection moved to the backend face: `Sts1PresentationBackend.combatFrame` projects hitbox geometry + localized label into `ControlsView` (`endTurnX/Y/W/H`, `endTurnLabel`, `hasEndTurnBounds`); render consumes projection via `ControlsDrawPath.endTurnProjectedBounds/endTurnLabel` with constant fallback only when projection is absent; bridge class deleted | Source review finding V-3 (render-path global-state read) | `Sts1EndTurnChrome.java` removed; `ControlsDrawPathTest.endTurnChromeReflectionBridgeRemoved` guards the class stays gone; projected/fallback geometry + label covered by pure JUnit | `ControlsViewEndTurnTest`, `ControlsDrawPathTest.projectedGeometryWinsOverConstantFallback`, `missingProjectionFallsBackToConstantBounds`, `projectedLabelFlowsIntoEndTurnDrawItem`, `endTurnChromeReflectionBridgeRemoved`; focused JUnit passed | Pending | complete |
