# Regression Baseline Ledger

| ID | Scope | Before | Target after | Evidence before | Evidence after | Tests | Review | Status |
|---|---|---|---|---|---|---|---|---|
| RB-01 | Presentation authority and lifecycle | ECS authority exists but its shared-world, context ownership, close, and reset invariants are spread across implementation and existing tests | One shared `PresentationWorld` is explicitly protected by focused tests for create, close, reset, recreation, and scope isolation; compatibility contexts do not retain mutable truth | `PresentationWorld`, `PresentationRegistry`, `PresentationContext`; `PresentationWorldTest` | Registry-owned world rejects direct close; `entity()` is read-only; reset clears retained ownership through the registry; focused lifecycle tests and full semantic gate pass. | `PresentationWorldTest`, `PresentationRegistryLifecycleTest`; `./scripts/with-art-env.sh test` | R6 `ses_fdc0f5ae1ffemBI2TOEBJOAUN4` found no source issue; final record closure | complete |
| RB-02 | Production schedule and compatibility bridge | Phase order and facade synchronous forwarding are tested, but the baseline must explicitly cover one schedule path and failure cleanup | All compatibility calls use the declared schedule; phase ordering and failed pending-frame cleanup remain regression-locked | `PresentationSchedule`, `FrameworkScheduleBridge`, `PresentationScheduleTest` | Added schedule-owned pulse-only execution; deprecated `LightwaveControls.tickPulses()` now delegates through it and does not run unrelated phases. Focused schedule/effect/node tests pass; full semantic gate and release gate pass. | `PresentationScheduleTest`, `EffectPulseSystemTest`, `LightwaveDemoContractTest`, `NodeConnectionsTest`; `./scripts/with-art-env.sh test`; `./scripts/release-gate.sh` | R3 independent review `ses_fdc4c127fffew1ol0grKXw6U3a` PASS | complete |
| RB-03 | Signal delivery boundary | SignalBus behavior is covered, but ordering, replacement, stop, and unmount cleanup need an explicit baseline row | Exact and regex subscriptions share deterministic order; replacement reaches later listeners; stop terminates delivery; cleanup removes scoped registrations | `SignalBus`, `SignalPaths`, `NodeConnections`; signal tests | Context-aware cleanup and transactional sync replacement added; failed initial sync disconnects staged subscriptions, while failed resync preserves previously working subscriptions. | `SignalBusTest`, `SignalApiContractTest`, `NodeConnectionsTest`; `failedResyncPreservesPreviouslyWorkingSubscriptions`; full semantic gate | R6 `ses_fdc0f5ae1ffemBI2TOEBJOAUN4` found no source issue; final record closure | complete |
| RB-04 | Render derivation and cache recreation | Render host caches are documented as derived from ECS, with several recreation tests already present | Clearing host caches and rebuilding produces the same targets, bounds, effects, and EntityPresent state without mutating ECS authority | `RenderHost`, `RenderPlan`, `RenderProjectionQueue`; `RenderPlanRebuildTest` | EntityPresent detach/clear immediately projects removal; public `bindEffect` rejects ECS-owned target kinds while internal planned projection restores ECS attachments; EntityPresent is explicitly effect-free and future FX must use separate ECS-owned visual/overlay entities. Focused render tests, full semantic gate, release gate, and consumer fixture pass. | `RenderPlanRebuildTest`, `RenderHostTest`, `HostRenderBackendTest`, `FullFrameTest`, `EntityPresentTest`; `./scripts/with-art-env.sh test`; `./scripts/release-gate.sh`; `./scripts/verify-consumer-fixture.sh` | R6 `ses_fdc0f5ae1ffemBI2TOEBJOAUN4` found no source issue; effect-free policy is documented and tested; final record closure | complete |
| RB-05 | Consumer contract and release checks | Public API stability and consumer fixture checks exist as scripts and docs | Public compatibility, Java 8 artifact, consumer fixture, and release gate are recorded as repeatable baseline evidence | `api-stability.md`, `consumer.md`, `release-gate.sh` | Consumer smoke added; dispatch documented; release gate verifies class major <= 52; current release gate and consumer fixture pass. | `ConsumerContractTest`; `./scripts/release-gate.sh`; `./scripts/verify-consumer-fixture.sh` | R6 `ses_fdc0f5ae1ffemBI2TOEBJOAUN4` found no source issue; final record closure | complete |
| RB-06 | Cross-boundary verification record | Existing project records contain historical test counts but not this baseline's exact gate results | Exact focused classes, full semantic result, consumer result, and scope check are persisted for this project | Existing CI/scripts and current worktree | Focused baseline and all subsequent focused slices pass; current full semantic gate, release gate, consumer fixture, and `git diff --check` pass after the latest source, test, design, and record changes. | `./scripts/with-art-env.sh test`; `./scripts/release-gate.sh`; `./scripts/verify-consumer-fixture.sh`; `git diff --check` | R6 `ses_fdc0f5ae1ffemBI2TOEBJOAUN4` reviewed current tree; project closure recorded | complete |
