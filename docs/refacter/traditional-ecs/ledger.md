# Traditional ECS before/after ledger

## Baseline Inventory

Captured 2026-08-16 by read-only repository inspection. Counts are navigation and trend evidence;
they are not completion criteria and must be recaptured before final project closure.

| Measure | Before | After | Interpretation |
|---|---:|---:|---|
| Main Java files | 395 | pending | Scope size only |
| Main Java lines | 40,247 | pending | Prefer reduced duplicate responsibility, not arbitrary line reduction |
| Test Java files | 112 | pending | Behavioral mapping matters more than count |
| Test methods | 577 | pending | Exact focused tests are recorded per row |
| Component classes | 67 | pending | Audit data-only fields and duplicate concepts |
| `EcsSystem` implementations | 12 | pending | Every production system needs explicit ownership and order |
| Production `EcsPipeline.run` call sites | 3 | pending | Classify schedule phases and synchronous command boundaries |
| Production immediate `projectNow()` calls | 17 | 14 source callers after TE-02 | Each remaining call is a synchronous command/lifecycle boundary; EntityPresent batching is TE-08 |
| Coverage instrumentation | none | pending | Use behavior matrix unless instrumentation is separately approved |

## Project Table

| ID | Scope | Before | Target after | Evidence before | Evidence after | Tests | Review | Status |
|---|---|---|---|---|---|---|---|---|
| TE-01 | Schedule and execution ownership | Production uses the central schedule plus direct pipeline execution whose ownership classification is not yet persisted | Every production phase is schedule-owned, or is a documented one-shot command boundary using schedule-owned stateless system instances | `PresentationSchedule`; production `EcsPipeline.run` inventory; direct surface lifecycle paths | 2026-08-16: the sole production `EcsPipeline.run` is the private `PresentationSchedule.run`; `SurfaceLifecycleSystem` is its fixed instance; authority-frame commands are destroyed in `finally` on projection failure | `PresentationScheduleTest.failedAuthorityProjectionDoesNotRetainItsPendingFrame`, `failedAdvanceFrameDoesNotRetainItsPendingAuthorityFrame`; `PresentSurfacesTest.facadeLifecycleCommandsAreConsumedByTheSchedule`; full `./scripts/with-art-env.sh test` passed | R1 findings R1-01/R1-02 fixed; focused re-review `ses_ff70a74c5ffeqvqniqsqugWgtd` PASS | complete |
| TE-02 | Render projection mutation boundary | Scheduled dirty projection coexists with immediate projection from lifecycle and render paths | Render writes and projection timing have one explicit contract; immediate paths are removed or justified with lifecycle tests | 2026-08-16 inventory: `RenderProjectionQueue`, `RenderHost`, 17 production `projectNow()` callers, and one STS draw-time `projectActiveSurfaces()` caller | Active projection filters C2 parent/items; C2 visuals are retained/cleaned per active owner and effects draw before content; pack mutation projects once; 14 `projectNow()` source callers remain | `RenderPlanRebuildTest.queueProjectsRequestedActiveSurface`; `HostRenderBackendTest.c2EffectBandDrawsOnlyActiveC2Targets`; full 581-test JUnit passed; verified D1 jar deployment then `d1_full_present_combat_ready` passed | R2 fixes PASS in `ses_ff65df0fdffeDRrWqfH2KrQnWO`; documented D1 smoke passed after one transient empty console response | complete |
| TE-03 | Compatibility stores and read purity | Compatibility containers include mutable state candidates; some nominal reads may synchronize ECS state | Compatibility views query ECS without retaining truth; commands and reads are visibly separated | 2026-08-16 baseline: `PresentSurfaces.BaseSurface.probeSlice()` invokes write-capable `syncComponents`; audit includes `PresentProjections`, `UiProbe`, `ArtFramework.OPEN`, inspect, and console reads | Surface/projection reads use existing-context/default snapshots; probe and inspect do not create C2 scopes or synchronize lifecycle; explicit mount/authority/reset remain writers | `UiOpsProbeTest.probeDoesNotCreateAbsentC2PresentationScopes`, `projectionReadsDoNotCreateAbsentProjectionScope`; `UiInspectTest.listSurfacesDoesNotCreateAbsentPresentSurfaceContext`; full 584-test JUnit passed | R3 `ses_ff4ee27d7ffeXw6wpySr4y4TKK` focused fix review PASS | complete |
| TE-04 | Component data-only and duplicate concepts | Component inventory has not been reviewed recursively against prohibited host values and duplicate identity/hierarchy/lifecycle concepts | Every persistent fact has one component owner; components contain immutable/data-only values; duplicate concepts are removed or justified | 2026-08-16 inventory: 71 component classes; unused duplicate `core` node identity/hierarchy/lifecycle/tree types; audit opaque `Object` snapshot/control fields and nested collections | Deleted four unused duplicate `core` node/tree components; `SurfacePolicyComponent` stores host-neutral level data with STS1 conversion at the facade boundary; generic value schemas split to TE-09 through TE-12 | Full 584-test JUnit passed; `PresentSurfacesTest.surfaceLifecycleAndPolicyAreMirroredByComponents`; negative searches find no deleted-type references or `context` component imports from `sts1` | R4 `ses_ff4beaf3cffe2bm4iCG0yUhcJq` focused fix review PASS | complete |
| TE-05 | Host cache recreation | Existing C1/C2/STS1 host and provider caches are intended to be disposable, but proof is distributed | Clearing each cache preserves ECS authority and recreation restores behavior from retained ECS state | 2026-08-16 inventory: RenderHost has explicit clear/recreate APIs and tests; skeleton bridge recreates bindings; audit StageHost actors, EffectTargetActors, STS1 texture cache, native templates, and EntityPresent host paths | `RenderHost.recreateHostCache` preserves ECS state; `PresentSafety` invalidates render/texture cache and rebuilds optional C1 actors from retained declarations. Stage/actor/Skin ownership is fail-closed; SkinOwnership releases active, retired, and shared host Skin instances exactly once. | `RenderPlanRebuildTest.hostRecreationReleasesCacheButRetainsEcsAuthority`; `SkinOwnershipTest` lifecycle cases; full `./scripts/with-art-env.sh test` passed 589/589; deployed D1 jar 940,143 bytes SHA-256 `de37ff59d9afee3d24d3bca988e3bdb68a0a9125663966645fd30ddf7811893d`; cold start READY, `art lab host-recreate`, and `d1_full_present_combat_ready` passed | R5 final closure `ses_ff1a4da5effeP6z6Wc4PLx16G3` PASS; accepted findings fixed and re-reviewed | complete |
| TE-06 | Dependency direction | Single-module compilation permits cross-package imports without build enforcement | Core ECS and presentation ownership stay independent of STS/native adapters; allowed inversions are explicit host boundaries | Initial import inventory found `context -> c2` in `SurfaceIds`/`MapGestureComponent` and `core -> c1` in `PresentPacks`/`PresentRestyle` | `MapNodeRef` and `NativeTemplateIds` now live in `artframework.component`; `context` and `core` have no direct C1/C2/STS1 imports. `PresentPacks` uses component loaders; C1 restyle actions use optional `PresentRestyleHost` adapter. Consumer fixture imports neutral IDs. | MapNodeRef review PASS; `scripts/verify-consumer-fixture.sh` passed; final `./scripts/with-art-env.sh test` passed 590/590 | R6 MapNodeRef review PASS; R7 stale consumer import fixed; final R7 closure `ses_ff0d41390ffegG3iR0XcOMUlzK` PASS | complete |
| TE-07 | Final negative inventory and closure | Legacy authority removal claims are distributed across design/task notes | No unexplained old writer, duplicate mutable store, bypass API, or stale compatibility path remains | Repeat searches and compare against baseline inventory | Production has one `EcsPipeline.run` in `PresentationSchedule`; deleted duplicate core node/tree classes have no references; no `context/core` host imports remain; remaining projection calls are classified schedule/lifecycle/host boundaries; compatibility caches show no second ECS authority in source review | Full `./scripts/with-art-env.sh test` passed 590/590; `scripts/verify-consumer-fixture.sh` passed; complete source inventory review PASS | TE-07 closure review PASS, session `ses_ff0c2a3aaffeuMBANViRjKcrNh`; residual cache boundaries are source-inspected | complete |
| TE-08 | EntityPresent atomic update contract | `attach`, `sync`, `layout`, and `detach` are independent synchronous APIs and each immediately projects; multi-call lab updates rebuild intermediate cache states | Decide whether independent synchronous visibility is the final contract or design an explicit atomic update/batch API with one final projection | R2-03; `NativeTemplateRuntime` render bridge; `ArtCommand entity-attach` sequence | Independent operations remain immediate compatibility APIs. New `EntityPresent.present` executes attach/sync/layout under a nested-safe projection batch and gives one final host-cache projection; console entity attach uses it. | `EntityPresentTest` creation/replacement lifecycle, standalone/outer-batch projection count; `RenderProjectionQueueTest` nested and reset cleanup; full `./scripts/with-art-env.sh test` passed 596/596 | R8 final closure `ses_ff0914405ffexJ8G3ZlnYKEKCh` PASS; listener exception rollback explicitly undefined | complete |
| TE-09 | C1 declaration and property value schema | `UiNode`, `C1DeclarationComponent`, and `NodePropertiesComponent` shallow-copy generic property values | Persistent declaration/property values use a documented recursively immutable, host-agnostic schema | R4-02/R4-08; `UiNode.props`; `NodePropertiesComponent` | `ImmutableUiValue` defines scalar/list/string-key-map schema; `UiNode`, effect params, and property overlays deep-copy and validate all retained values; cycles and host objects are rejected | `ImmutableUiValueTest`; `TemplateExpanderTest.nestedCallPropertiesAreCopiedAndValidated`; full `./scripts/with-art-env.sh test` passed 608/608 | TE-09 final closure `ses_ff05c2e77ffevblrABVBp8BWPM` PASS | complete |
| TE-10 | EntityPresent snapshot value schema | `EntitySlotSnapshotComponent` stores the caller's unconstrained `Object` reference | Entity snapshots are framework-owned immutable values with no native/game object retention | R4-03; `EntityPresent.sync`; `EntitySnapshot` | `sync`/`present` normalize `null`, typed snapshots, and string-key maps before ECS write; component/view retain typed `EntitySnapshot`; extras use `ImmutableUiValue`; unsupported ingress fails | `EntityPresentTest` mutable map/typed extras isolation and prohibited-value rejection; `EntityDrawPathTest` compatibility map/unknown read fallback; `./scripts/with-art-env.sh test` passed 612/612 | R11 findings fixed; closure review `ses_ff0237f7effeWyq4sRZCeThc21` PASS | complete |
| TE-11 | Control value schema | `ControlValueComponent` stores arbitrary `Object` values from materialization and UiOps | Control state accepts only explicit immutable scalar/value types used by supported controls | R4-04; C1 materializer, UiOps, WidgetSession, ComponentActors, UiProbe | `ControlValueComponent` accepts only finite `Float`, `String`, or `Boolean`; `ControlBoundsComponent` materializes finite ranges once; mounted runtime consumers use ECS bounds; UiOps/WidgetSession reject non-finite requests; declaration values use finite fallback | `PresentationRuntimeTest` scalar/rejection, finite initialization, and bounds-after-property-change; `WidgetSessionTest` direct finite rejection and materialized bounds; `UiOpsProbeTest` rejected request/probe safety; `./scripts/with-art-env.sh test` passed 621/621 | R12-01 through R12-04 fixed; closure review `ses_fefcff102ffehaseJ1cP2LpRAO` PASS | complete |
| TE-12 | Intent and connection declaration schemas | Surface intent args and connection maps shallow-copy generic nested values | One-shot intent and persistent connection data use validated recursively immutable host-agnostic values | R4-05/R4-06; `SurfaceIntentExecutionComponent`; `ConnectionDeclarationsComponent` | Intent args and connection declarations deep-copy via `ImmutableUiValue`; intent-only `CardRef`/`MapNodeRef` recursively encode to reserved wire envelopes and restore before dispatch; subscriptions/actions receive immutable args; dispatch cleanup is `finally` | `SurfaceIntentExecutionSystemTest` nested mutation/reference dispatch, host rejection, reserved-key rejection, failure cleanup; `NodeConnectionsTest` nested declaration and listener args isolation; `./scripts/with-art-env.sh test` passed 629/629 with 0 failures, 0 errors, 0 ignored | R13 findings fixed and focused closure review `ses_fed776d3fffen6UHqPbYmd3Efl` PASS; `NodeStateMachine` boundary split to TE-13 | complete |
| TE-13 | NodeStateMachine action declaration schema | State-machine transition and enter-action maps shallow-copy nested values | State-machine action declarations use the same recursively immutable schema as C1 connections | R12-03; `NodeStateMachine.Transition`; `setEnterActions` | `Transition.onEnter` and `setEnterActions` normalize every action spec through `ImmutableUiValue`; nested caller mutations cannot alter retained declarations; unsupported host values are rejected at ingress | `NodeStateMachineTest.transitionAndStateEnterActionsCopyNestedArgs`, `stateMachineActionsRejectHostValues`, `transitionActionsCopyNestedArgs`, `transitionActionsRejectHostValues`; `./scripts/with-art-env.sh test` passed 633/633 with 0 failures, 0 errors, 0 ignored | R14-01 fixed and focused closure review `ses_fed706ae2ffepArOrs8h97Xutk` PASS after re-review | complete |
| TE-14 | PresentPack operation runtime | `PresentPacks` directly mutates ECS-adjacent registries and host caches through field-specific activation code | Pack declarations execute reversible, dependency-checked operations through one runtime boundary; component, system, asset, template, and window domains retain their own data authority | 2026-08-18: `PresentPacks.activePackId` and `PresentPackApply` bookkeeping drive activation; `PresentationWorld` has no operation transaction API; systems and HostAssets are separate static/runtime paths | `PackWorld` owns applied-operation undo logs across ECS and HostAssets; `PackSystems` supplies fixed-phase extensions; `PresentPacks` calls the runtime and snapshots/restores legacy template/window replacements on failed activation. Legacy declaration fields remain a separately tracked migration. | `PackWorldTest` rollback, dependency, duplicate-system, and production-schedule cases; `PresentPackTest` failure restoration and constructor compatibility; final `./scripts/with-art-env.sh test` passed 643/643, 0 failures/errors/ignored | R15 findings TE-14-01 through TE-14-04 fixed; closure re-review `ses_fec6dfe63ffeOjw8psACMBg7WM` PASS | complete |
| TE-15 | Lightwave C1 default-effect declaration migration | `effectDefaults` is read from `PresentPackApply` during C1 materialization and title render planning | Enabled pack effect defaults are data-only ECS contributions created and removed by a PackWorld operation; C1 reads ECS first | TE-14 closure: legacy declarations are snapshot-reversible but not yet operation-log entries; `C1Materializer` and `RenderPlan` directly read `PresentPackApply.effectDefaultsForType` | `PackEffectDefaultsComponent` is created/removed by the normalized PresentPack operation for builder and legacy constructor paths; C1 materializer and title render read shared ECS first; generic Component CRUD rejects the reserved contribution type; `hasContribution` suppresses legacy fallback even for empty ECS contributions; legacy facade is fallback only when no migrated contribution exists | `PackEffectDefaultsTest` and `PresentPackTest` focused lifecycle, constructor, duplicate/foreign-owner, reserved-writer, C1 materializer, and RenderPlan fallback cases; final `./scripts/with-art-env.sh test` passed 648/648, 0 failures/errors/ignored | Initial review `ses_fec440784ffeq6ueWYJWoYzX6Y` findings TE-15-01/02 fixed; follow-up TE-15-03 fixed; closure review `ses_fec15e3a2ffe3IkJDKeE7v6oIJ` raised TE-15-04, fixed and consumer-tested; independent closure review `ses_fec046d66ffeJyPY9JKXGRlqYL` PASS | complete |
| TE-16 | C2 surface-effect declaration migration | `surfaceEffects` is read from `PresentPackApply` and `PresentationVisuals` as active-pack field data | Enabled pack C2 surface effects are data-only ECS contributions created and removed by a PackWorld operation; C2 visual item materialization reads ECS first | TE-15 closure; `PresentPackApply.applyC2SurfaceEffects` writes render state from `pack.surfaceEffects`; `PresentationVisuals.effectsFor` reads `PresentPacks.active().surfaceEffects` | `PackSurfaceEffectsComponent` is created/removed by normalized PresentPack operations; `PresentationVisuals` and `PresentPackApply` read ECS contribution first and only fall back when no migrated contribution exists; `PresentPackApply` iterates ECS-contributed surface IDs, not legacy field keys; item resync removes stale prior pack effects before applying current contribution; generic Component CRUD rejects the reserved type | Focused `PackSurfaceEffectsTest`, `C2LightwaveSurfaceTest`, and `PresentationVisualsTest` passed; final `./scripts/with-art-env.sh test` passed 653/653, 0 failures/errors/ignored | Initial review `ses_febe8845cffeRibFYWggCODumL` finding TE-16-01 fixed; independent closure review `ses_febccdc99ffezoyu6y3HBfq1Uv` PASS after stale-effect cleanup fix | complete |
| TE-17 | Full-frame effect declaration migration | `fullFrameEffects` is read from `PresentPackApply` and directly projected from active pack field data | Enabled pack full-frame effects are immutable ECS contributions created and removed by a PackWorld operation; full-frame projection reads ECS first | TE-16 closure; `PresentPackApply.applyFullFrame` loops over `pack.fullFrameEffects` and manages host projection separately | `PackFullFrameEffectsComponent` is created/removed by normalized PresentPack operations; resolver filters by active pack owner; `PresentPackApply` reads owner-scoped ECS contribution first; deactivation clears active/host projection even when undo fails; reset discards irrecoverable failed undo records; generic CRUD rejects the reserved type | `PackFullFrameEffectsTest`, `PresentPackFullFrameLifecycleTest`, `PresentPackTest`, and `FullFrameTest` focused cases passed; final `./scripts/with-art-env.sh test` passed 656/656, 0 failures/errors/ignored | Initial review `ses_feb7459e1ffeD5uau4E9RQa3cI` findings TE-17-01/02 fixed; independent closure review `ses_feb688398ffeUrHe3cHiRN6j4f` PASS | complete |
| TE-18 | Bind-surface declaration migration | `bindSurfaces` remains a field-specific `PresentPackApply` mutation with static cleanup bookkeeping | Surface profile bindings are reversible PackWorld operations with explicit ownership and cleanup | TE-17 closure; `PresentPackApply.applySurfaceBinds` loops over `pack.bindSurfaces` and writes `SurfacePresent` with static `BOUND_SURFACES` cleanup | `PackSurfaceBindingsComponent` is created/removed by normalized PresentPack operations; `PresentPackApply` projects owner-scoped ECS bindings first and legacy fields only when no migrated contribution exists; empty owner contributions suppress fallback; profile IDs normalize at pack ingress; cleanup restores prior SurfacePresent bindings; `SurfacePresent` remains the ECS-backed host projection | Focused `PackSurfaceBindingsTest`, `PresentPackTest`, `C2LightwaveSurfaceTest`, and `LightwaveCoverageTest` passed; `PackSurfaceBindingsTest` covers reserved create/update CRUD rejection; final `./scripts/with-art-env.sh test` passed 665/665, 0 failures/errors/ignored | Initial review findings R19-01/R19-02/R19-03 fixed; R20-01 test gap fixed; closure review `ses_feaf2772fffefqWiEkMCH89Rt4` PASS | complete |
| TE-19 | Template/window registration migration | `PresentPacks.ensureLoaded` and snapshot adapters remain outside direct operation log | Template/window registration and restoration use the unified operation protocol | `PresentPacks.ensureLoaded` directly loaded classpath templates and mutated `ComponentRegistry`/`ArtFramework`; `OWNED_*` and `PREVIOUS_*` maps were a second restoration ledger | `PresentPack` normalizes template/window declarations into `PackWorld` operations; resource parsing and registration run transactionally; operation undo restores previous registrations, while deactivate flags control normal cleanup and failed enable always forces rollback; `PresentPacks` no longer owns registration snapshot maps; post-enable sync failure uses forced abort and preserves the original exception | `PresentPackTest.declarationsBecomeOperationsAndRespectDeactivateRegistrationPolicies`, existing activation/replacement/failure cases; final `./scripts/with-art-env.sh test` passed 665/665, 0 failures/errors/ignored | Independent closure review `ses_feb0dfa7cffe9xW4r6c5u1WOtY` PASS with no findings | complete |

| TE-20 | Pack projection ownership and ECS cleanup boundary | `PresentPackApply` keeps static surface-binding/effect/full-frame cleanup state while writing ECS-backed presentation state; projection is not consistently scoped to the active pack owner | Pack activation/deactivation has one owner-aware projection and cleanup boundary; pre-existing or externally updated ECS presentation state survives pack cleanup; failed projection leaves no stale pack projection | R20 closure plus TE-20 review `ses_fead5f2adffe2amOrVmnQElV5L`: static cleanup records, global contribution lookup, and abort without forced projection cleanup | Active owner lookup is scoped; legacy fallback paths record applied values; cleanup compares current state before restoring, preserves interleaved non-pack writers, and retains failed binding cleanup records for retry; failed activation aborts then reprojects no-active state. | `PackSurfaceEffectsTest`, `PackSurfaceBindingsTest`, `PackFullFrameEffectsTest`, `PresentPackTest.failedActivationReprojectsTheNoActivePackState`; full `./scripts/with-art-env.sh test` passed | R21 findings TE-20-01..04 fixed; follow-up TE-20-05..07 fixed; TE-20-08 fix review `ses_fead5f2adffe2amOrVmnQElV5L` PASS | complete |

## Status Rules

- `pending`: not investigated
- `reviewing`: baseline or findings in progress
- `implementing`: accepted findings being fixed
- `verifying`: implementation exists and gates are running
- `blocked`: named prerequisite or decision prevents progress
- `complete`: after evidence, tests, gates, and review closure are recorded

Do not overwrite the before state after implementation. Add concise evidence to `Evidence after` and
record exact test classes or scenarios rather than writing only `tests pass`.
