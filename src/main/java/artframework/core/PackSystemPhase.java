package artframework.core;

/** Fixed schedule extension points available to enabled PresentPack systems. */
public enum PackSystemPhase {
    WORLD_NORMALIZATION,
    ANIMATION,
    EFFECTS,
    HOST_PRESENTATION,
    RENDER_PROJECTION
}
