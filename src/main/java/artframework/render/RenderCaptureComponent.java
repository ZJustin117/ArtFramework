package artframework.render;

/** Data-only capture policy for the render host. */
public final class RenderCaptureComponent {
    public final boolean enabled;

    public RenderCaptureComponent(boolean enabled) {
        this.enabled = enabled;
    }
}
