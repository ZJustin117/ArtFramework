# Device: FULL combat request safely falls back while start-run has not reached combat.
# A later combat-room scenario validates the FULL_READY branch.
name: d1_full_present_combat_on
schemaVersion: 1
mode: device
device: d1
require:
  env:
    - ART_D1_SERIAL
    - STS_CONNECTOR_PORT
    - SLAY_THE_AMETHYST_ROOT
steps:
  - console: "art lab ensure-fresh-menu"
  - wait_ms: 8000
  - console: "art lab start-run IRONCLAD"
  - wait_ms: 25000
  - console: "art present combat on"
  - wait_ms: 1000
  - console: "art frame"
  - wait_ms: 600
  - console: "art present status"
  - wait_ms: 400
  - probe: all
  - assert:
      path: schemaVersion
      eq: 1
  - assert:
      path: modId
      eq: artframework
  - assert:
      path: backend.id
      eq: signals
  - assert:
      path: backend.fullPresent.combatHand
      eq: "FULL"
  - assert:
      path: backend.fullPresent.combatHandFull
      eq: true
  - assert:
      path: backend.fullPresent.maySuppressNativeHand
      eq: false
  - assert:
      path: backend.input.handOwnsInput
      eq: false
  - assert:
      path: backend.input.handState
      eq: "FULL_FALLBACK_NATIVE"
  - assert:
      path: backend.renderPlan
      exists: true
  - assert:
      path: backend.controlsDraw
      exists: true
