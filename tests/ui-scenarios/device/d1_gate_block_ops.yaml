# Device: lab gate BLOCK → map/endturn ops report BLOCKED (intercept path).
# Requires ArtFramework loaded + game READY (android-device-lab.md).
name: d1_gate_block_ops
schemaVersion: 1
mode: device
device: d1
require:
  env:
    - ART_D1_SERIAL
    - STS_CONNECTOR_PORT
    - SLAY_THE_AMETHYST_ROOT
steps:
  - console: "art gate all clear"
  - wait_ms: 200
  - console: "art gate map block"
  - wait_ms: 200
  - console: "art op map 1 0 monster"
  - wait_ms: 300
  - console: "art gate endturn block"
  - wait_ms: 200
  - console: "art op endturn"
  - wait_ms: 300
  - console: "art gate all clear"
  - wait_ms: 200
  - probe: all
  - assert:
      path: schemaVersion
      eq: 1
  - assert:
      path: modId
      eq: artframework
  - assert:
      path: templates.mapBound
      eq: true
  - assert:
      path: templates.endTurnBound
      eq: true
