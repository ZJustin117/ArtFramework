# Device smoke placeholder: requires ART_D1_SERIAL + future art probe.
# Offline runs skip when env missing; do not use as default gate.
name: s1_mod_loaded
schemaVersion: 1
mode: device
require:
  env:
    - ART_D1_SERIAL
steps:
  - probe: all
  - assert:
      path: schemaVersion
      eq: 1
  - assert:
      path: modId
      eq: artframework
